# GridPulse dependencies. Pinned to minimum-compatible versions rather
# than exact `pip freeze` output — this project runs on system Python on
# Windows locally but must also install cleanly on GitHub Actions' Ubuntu
# runner, and exact Windows-captured pins (plus Windows-only transitive
# packages like pywin32) don't resolve there. See docs/build-log.md /
# commit history for the 2026-08-18 pywin32 incident this replaces.

# Ingestion (Phase 1)
requests>=2.31
openpyxl>=3.1
xlrd>=2.0

# Database (Phase 2)
SQLAlchemy>=2.0
psycopg2-binary>=2.9
python-dotenv>=1.0

# Data handling (all phases)
pandas>=2.0
numpy>=1.24

# Statistical analysis notebook (Phase 3)
jupyter>=1.0
nbformat>=5.9
nbconvert>=7.0
matplotlib>=3.7
seaborn>=0.12
statsmodels>=0.14
scipy>=1.10

# Kaggle backfill fallback (Phase 1, report.grid-india.in outage)
kaggle>=1.6

# Tests
pytest>=7.4
